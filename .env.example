# Copy this file to .env and fill in your values.
# The values below match the defaults in docker-compose.yml.

# ── Database ──────────────────────────────────────────────────────
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pop_os"

# ── Web server ────────────────────────────────────────────────────
PORT=3000
APP_URL="http://192.168.1.40:3000"   # public URL used in welcome emails

# ── JWT auth ──────────────────────────────────────────────────────
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="replace-this-with-a-long-random-string-in-production"

# ── Autocount Cloud accounting integration ────────────────────────
# Find these in Autocount Cloud → Settings → API Integration.
AUTOCOUNT_BASE_URL="https://accounting-api.autocountcloud.com"
AUTOCOUNT_ACCOUNT_BOOK_ID="your-account-book-id"
AUTOCOUNT_KEY_ID="your-key-id"
AUTOCOUNT_API_KEY="your-api-key"
AUTOCOUNT_DEFAULT_LOCATION="HQ"
AUTOCOUNT_DEFAULT_CREDIT_TERM="C.O.D."

# ── Email notifications (optional — payment alert emails) ────────────────
# Any SMTP provider works: Gmail, Outlook, Mailgun, etc.
# Gmail: use an App Password (not your login password) with SMTP_HOST=smtp.gmail.com SMTP_PORT=587
# Leave blank to disable email alerts (in-app alerts still work).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Pop OS <noreply@pop.studio>"
ALERT_EMAIL_TO=""      # comma-separated recipients, e.g. "finance@pop.studio,yj@pop.studio"
ALERT_DAYS="10"        # alert when invoice due within this many days

# ── WhatsApp lead-status notifications (optional) ─────────────────────────
# Uses whatsapp-web.js — an unofficial client that links a real WhatsApp
# number (use a dedicated/burner number, already a member of the target
# group; automating a personal number risks a ban).
# First run: set WHATSAPP_ENABLED=true (WHATSAPP_GROUP_ID can stay blank —
# not known yet), start the server, scan the QR code printed in the console
# (WhatsApp → Linked Devices → Link a Device). Once connected the console
# logs every group the number is in with its ID — copy the right one below
# and restart.
WHATSAPP_ENABLED="false"
WHATSAPP_GROUP_ID=""   # e.g. "123456789012345678@g.us"
# Optional — point at a system Chromium instead of downloading one (useful
# on the Debian server; see CLAUDE.md deploy notes for apt deps needed).
PUPPETEER_EXECUTABLE_PATH=""

# ── Airtable import (optional — only needed for one-time data migration) ──
AIRTABLE_PAT=""
